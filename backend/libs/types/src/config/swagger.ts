export interface ISwaggerConfig {
  title: string;
  description: string;
  version: string;
}

export enum SwaggerConfigEnum {
  TITLE = 'title',
  DESCRIPTION = 'description',
  VERSION = 'version',
}
